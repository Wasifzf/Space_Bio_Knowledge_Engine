# Frontend environment variables
# Copy this file to .env and adjust values as needed.

# Base URL for backend API (FastAPI). Should match uvicorn host/port.
VITE_API_BASE_URL=http://localhost:8000

# Optionally toggle debug banners or feature flags (add as needed)
# VITE_FEATURE_SHOW_EXPERIMENTAL=true
